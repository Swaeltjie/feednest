engine-strict=true
# vite 8 / @sveltejs/vite-plugin-svelte 7 are newer than the vite/plugin range
# the current @sveltejs/kit declares as peers; the combination works in practice.
# Required so `npm ci` (Docker build, CI) resolves the same tree as local installs.
legacy-peer-deps=true
